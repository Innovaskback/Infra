apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-sharing-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ba-sharing-prod
  template:
    metadata:
      labels:
        app: ba-sharing-prod
    spec:
      containers:
        - name: sharing-prod
          image: zednytawab/sharing:be-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-sharing-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: ba-sharing-prod
  ports:
    - name: sharing-prod
      protocol: TCP
      port: 8080
      targetPort: 8080
