apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-mindmap-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ba-mindmap-prod
  template:
    metadata:
      labels:
        app: ba-mindmap-prod
    spec:
      containers:
        - name: mindmap-prod
          image: zednytawab/mindmap:be-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-mindmap-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: ba-mindmap-prod
  ports:
    - name: mindmap-prod
      protocol: TCP
      port: 8080
      targetPort: 8080
