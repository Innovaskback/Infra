apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-quizzes-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ba-quizzes-prod
  template:
    metadata:
      labels:
        app: ba-quizzes-prod
    spec:
      containers:
        - name: quizzes-prod
          image: zednytawab/quizzes:be-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-quizzes-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: ba-quizzes-prod
  ports:
    - name: quizzes-prod
      protocol: TCP
      port: 8080
      targetPort: 8080
