apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-videoplayer-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-videoplayer-test
  template:
    metadata:
      labels:
        app: ba-videoplayer-test
    spec:
      containers:
        - name: videoplayer-test
          image: zednytawab/videoplayer:be-te
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-videoplayer-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: ba-videoplayer-test
  ports:
    - name: videoplayer-test
      protocol: TCP
      port: 8080
      targetPort: 8080
