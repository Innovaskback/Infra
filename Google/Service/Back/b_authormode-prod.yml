apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-authormode-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ba-authormode-prod
  template:
    metadata:
      labels:
        app: ba-authormode-prod
    spec:
      containers:
        - name: authormode-prod
          image: zednytawab/authormode:be-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-authormode-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: ba-authormode-prod
  ports:
    - name: authormode-prod
      protocol: TCP
      port: 8080
      targetPort: 8080
