apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-dashboard-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ba-dashboard-prod
  template:
    metadata:
      labels:
        app: ba-dashboard-prod
    spec:
      containers:
        - name: dashboard-prod
          image: zednytawab/dashboard:be-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-dashboard-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: ba-dashboard-prod
  ports:
    - name: dashboard-prod
      protocol: TCP
      port: 8080
      targetPort: 8080
