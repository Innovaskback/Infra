apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-mindmap-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-mindmap-test
  template:
    metadata:
      labels:
        app: ba-mindmap-test
    spec:
      containers:
        - name: mindmap-test
          image: zednytawab/mindmap:be-te
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-mindmap-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: ba-mindmap-test
  ports:
    - name: mindmap-test
      protocol: TCP
      port: 8080
      targetPort: 8080
