apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-oldzedny-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe-oldzedny-test
  template:
    metadata:
      labels:
        app: fe-oldzedny-test
    spec:
      containers:
        - name: oldzedny
          image: zednytawab/oldzedny:fe-te
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-oldzedny-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: fe-oldzedny-test
  ports:
    - name: oldzedny
      protocol: TCP
      port: 3000
      targetPort: 3000
