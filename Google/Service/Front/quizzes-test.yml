apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-quizzes-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe-quizzes-test
  template:
    metadata:
      labels:
        app: fe-quizzes-test
    spec:
      containers:
        - name: quizzes-test
          image: zednytawab/quizzes:fe-te
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-quizzes-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: fe-quizzes-test
  ports:
    - name: quizzes-test
      protocol: TCP
      port: 3000
      targetPort: 3000
