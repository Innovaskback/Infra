apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-dashboard-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe-dashboard-test
  template:
    metadata:
      labels:
        app: fe-dashboard-test
    spec:
      containers:
        - name: dashboard-test
          image: zednytawab/dashboard:fe-te
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-dashboard-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: fe-dashboard-test
  ports:
    - name: dashboard-test
      protocol: TCP
      port: 3000
      targetPort: 3000
