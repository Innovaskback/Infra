apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-admin-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-admin-prod
  template:
    metadata:
      labels:
        app: fe-admin-prod
    spec:
      containers:
        - name: admin-prod
          image: zednytawab/admin:fe-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-admin-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fe-admin-prod
  ports:
    - name: admin-prod
      protocol: TCP
      port: 3000
      targetPort: 3000
