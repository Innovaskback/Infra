apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-infographic-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe-infographic-test
  template:
    metadata:
      labels:
        app: fe-infographic-test
    spec:
      containers:
        - name: infographic-test
          image: zednytawab/infographic:fe-te
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-infographic-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: fe-infographic-test
  ports:
    - name: infographic-test
      protocol: TCP
      port: 3000
      targetPort: 3000
