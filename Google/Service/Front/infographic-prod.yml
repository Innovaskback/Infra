apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-infographic-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-infographic-prod
  template:
    metadata:
      labels:
        app: fe-infographic-prod
    spec:
      containers:
        - name: infographic-prod
          image: zednytawab/infographic:fe-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-infographic-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fe-infographic-prod
  ports:
    - name: infographic-prod
      protocol: TCP
      port: 3000
      targetPort: 3000
