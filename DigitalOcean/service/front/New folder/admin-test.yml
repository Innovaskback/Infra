apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-admin-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe-admin-test
  template:
    metadata:
      labels:
        app: fe-admin-test
    spec:
      containers:
        - name: admin
          image: zednytawab/admin:fe-te
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-admin-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: fe-admin-test
  ports:
    - name: admin
      protocol: TCP
      port: 3000
      targetPort: 3000
