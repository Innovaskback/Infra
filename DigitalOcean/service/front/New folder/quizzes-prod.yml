apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-quizzes-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-quizzes-prod
  template:
    metadata:
      labels:
        app: fe-quizzes-prod
    spec:
      containers:
        - name: quizzes-prod
          image: zednytawab/quizzes:fe-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-quizzes-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fe-quizzes-prod
  ports:
    - name: quizzes-prod
      protocol: TCP
      port: 3000
      targetPort: 3000
