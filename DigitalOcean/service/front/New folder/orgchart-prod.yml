apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-orgchart-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-orgchart-prod
  template:
    metadata:
      labels:
        app: fe-orgchart-prod
    spec:
      containers:
        - name: orgchart-prod
          image: zednytawab/orgchart:fe-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-orgchart-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fe-orgchart-prod
  ports:
    - name: orgchart-prod
      protocol: TCP
      port: 3000
      targetPort: 3000
