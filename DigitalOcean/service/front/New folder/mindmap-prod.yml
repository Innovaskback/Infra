apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-mindmap-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-mindmap-prod
  template:
    metadata:
      labels:
        app: fe-mindmap-prod
    spec:
      containers:
        - name: mindmap-prod
          image: zednytawab/mindmap:fe-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-mindmap-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fe-mindmap-prod
  ports:
    - name: mindmap-prod
      protocol: TCP
      port: 3000
      targetPort: 3000
