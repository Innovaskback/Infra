apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-authormode-prod
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-authormode-prod
  template:
    metadata:
      labels:
        app: fe-authormode-prod
    spec:
      containers:
        - name: authormode-prod
          image: zednytawab/authormode:fe-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-authormode-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fe-authormode-prod
  ports:
    - name: authormode-prod
      protocol: TCP
      port: 3000
      targetPort: 3000
