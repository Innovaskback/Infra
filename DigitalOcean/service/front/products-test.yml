apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-products-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fe-products-test
  template:
    metadata:
      labels:
        app: fe-products-test
    spec:
      containers:
        - name: products-test
          image: zednytawab/products:fe-te
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-products-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: fe-products-test
  ports:
    - name: products-test
      protocol: TCP
      port: 3000
      targetPort: 3000
