apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-prelogin-ksa
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ba-prelogin-ksa
  template:
    metadata:
      labels:
        app: ba-prelogin-ksa
    spec:
      containers:
        - name: prelogin-ksa
          image: zednytawab/prelogin:be-ksa
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-prelogin-ksa
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: ba-prelogin-ksa
  ports:
    - name: prelogin-ksa
      protocol: TCP
      port: 8080
      targetPort: 8080
