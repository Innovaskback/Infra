apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-dashboard-ksa
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-dashboard-ksa
  template:
    metadata:
      labels:
        app: fe-dashboard-ksa
    spec:
      containers:
        - name: dashboard-ksa
          image: zednytawab/dashboard:fe-ksa
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-dashboard-ksa
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fe-dashboard-ksa
  ports:
    - name: dashboard-ksa
      protocol: TCP
      port: 3000
      targetPort: 3000
