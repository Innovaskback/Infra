apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-admin-ksa
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-admin-ksa
  template:
    metadata:
      labels:
        app: fe-admin-ksa
    spec:
      containers:
        - name: admin-ksa
          image: zednytawab/admin:fe-ksa
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: fe-admin-ksa
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: fe-admin-ksa
  ports:
    - name: admin-ksa
      protocol: TCP
      port: 3000
      targetPort: 3000
