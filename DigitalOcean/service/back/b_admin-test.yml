apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-admin-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-admin-test
  template:
    metadata:
      labels:
        app: ba-admin-test
    spec:
      containers:
        - name: admin
          image: zednytawab/admin:be-te
          imagePullPolicy: Always         
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-admin-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: ba-admin-test
  ports:
    - name: admin
      protocol: TCP
      port: 8080
      targetPort: 8080
