apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-admin-prod
  namespace: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-admin-prod
  template:
    metadata:
      labels:
        app: ba-admin-prod
    spec:
      containers:
        - name: admin-prod
          image: zednytawab/admin:be-pr
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-admin-prod
  namespace: production
spec:
  type: ClusterIP
  selector:
    app: ba-admin-prod
  ports:
    - name: admin-prod
      protocol: TCP
      port: 8080
      targetPort: 8080
