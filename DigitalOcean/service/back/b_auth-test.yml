apiVersion: apps/v1
kind: Deployment
metadata:
  name: ba-auth-test
  namespace: testing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ba-auth-test
  template:
    metadata:
      labels:
        app: ba-auth-test
    spec:
      containers:
        - name: auth-test
          image: zednytawab/authentication:be-te
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: ba-auth-test
  namespace: testing
spec:
  type: ClusterIP
  selector:
    app: ba-auth-test
  ports:
    - name: auth-test
      protocol: TCP
      port: 8080
      targetPort: 8080
